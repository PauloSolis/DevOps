name: My First workflow

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
    myfirstjob:
              name: First job
              runs-on: ubuntu-latest
              steps:
              - name: Step one
                uses: actions/checkout@v3
              - name: Step two
                run: |
                  touch newFile.yaml
                  echo "Please let me leaaave" >> newFile.yaml
                  cat newFile.yaml
                  pwd
    mysecondjob:
                needs: [myfirstjob]
                name: Second job
                runs-on: macos-latest
                steps:
                  - name: Step one
                    run: date
    mythirdjob:
                name: Third job
                runs-on: windows-latest
                steps:
                  - name: Step one
                    run: date
